function buyNow(productName, price) {
    alert(`Você escolheu ${productName} por R$${price.toFixed(2)}! Redirecionando para o pagamento...`);
    // Aqui você pode integrar um sistema de pagamento, como PayPal ou Stripe.
}
